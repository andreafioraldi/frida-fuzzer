var config = require("./config.js");


function has_new_bits(

function save_if_interesting (buf) {
  
  has_new_bits

}


exports.common_fuzz_stuff(buf, callback) {

  // zero the bitmap here

  callback(buf);
  
  save_if_interesting(buf);

}
